// Reexport tipado de la config compartida para el código de la app (TSX).
// La fuente única está en ../site.config.mjs (la lee también next.config.mjs).
export { SITE, BASE_PATH as BP, ORIGIN } from "../site.config.mjs";
